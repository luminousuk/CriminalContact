<Page x:Class="CriminalContact.UWP.Views.SummaryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Behaviors="using:Template10.Behaviors"
      xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
      xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:controls="using:Template10.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:CriminalContact.UWP.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:CriminalContact.UWP.ViewModels" mc:Ignorable="d">

    <Page.DataContext>
        <vm:SummaryPageViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <controls:PageHeader x:Name="pageHeader" RelativePanel.AlignLeftWithPanel="True"
                             RelativePanel.AlignRightWithPanel="True"
                             RelativePanel.AlignTopWithPanel="True" Text="Home">
        </controls:PageHeader>


        <TextBlock x:Name="gameStatusTextBox" Margin="16,16,0,0"
                   RelativePanel.AlignLeftWithPanel="true"
                   RelativePanel.Below="pageHeader"
                   Text="{Binding GameStatusText, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}" />

    </RelativePanel>
</Page>
